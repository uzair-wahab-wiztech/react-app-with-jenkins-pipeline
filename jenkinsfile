pipeline {
    agent {
        docker "alpine"
    }

    stages {
        stage('Build') {
            steps {
                echo 'hello first jenkins pipeline'
            }
        }
    }
}