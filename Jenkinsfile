pipeline {
    agent {
        docker {
            image "alpine"
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'hello first jenkins pipeline'
            }
        }
    }
}